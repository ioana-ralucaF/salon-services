import { SelectedAddonsPipe } from './selected-addons.pipe';

describe('SelectedAddonsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedAddonsPipe();
    expect(pipe).toBeTruthy();
  });
});
